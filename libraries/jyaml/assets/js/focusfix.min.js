/*! JYAML Template Framework (http://www.jyaml.de), License: Creative Commons Attribution 3.0 (http://www.jyaml.de/en/license) */
var YAML_focusFix = {
    skipClass: "ym-skip",
    init: function() {
        if (!YAML_focusFix.loaded) {
            var a = navigator.userAgent.toLowerCase(),
                b = -1 < a.indexOf("webkit"),
                a = -1 < a.indexOf("msie");
            if (b || a) b = document.body, a = YAML_focusFix.click, b.addEventListener ? b.addEventListener("click", a, !1) : b.attachEvent && b.attachEvent("onclick", a);
            YAML_focusFix.loaded = !0
        }
    },
    trim: function(a) {
        return a.replace(/^\s\s*/, "").replace(/\s\s*$/, "")
    },
    click: function(a) {
        a = a || window.event;
        a = a.target || a.srcElement;
        for (var b = a.className.split(" "), c = 0; c <
            b.length; c++)
            if (YAML_focusFix.trim(b[c]) === YAML_focusFix.skipClass) {
                YAML_focusFix.focus(a);
                break
            }
    },
    focus: function(a) {
        if (a.href && (a = a.href, a = a.substr(a.indexOf("#") + 1), a = document.getElementById(a))) a.setAttribute("tabindex", "-1"), a.focus()
    },
    loaded: null
};